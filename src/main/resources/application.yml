spring:


  logging:
    level:
      org.hibernate.SQL: debug
  #      org.hibernate.type: trace

  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher


logging:
  level:
    org:
      springframework:
        boot:
          autoconfigure: ERROR
#      apache:
#        coyote:
#          http11:
#            debug

server:
  port: 8080
  servlet:
    encoding:
      charset: utf-8
      enabled: true
      force: true